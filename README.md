# agent-of-the-island
Knowledge system for the strong hearted

# Run the pipeline
```
python3 -m venv .island
```

- Swtich to .island environment and run:
```
pip install -r requirements.txt
```
- Now to create Vector DB and assimilate inference, run:
```
python app.py
```
